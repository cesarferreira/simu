{
  "include": ["./.base_robin.json"],
  "scripts": {
    "list": "cargo run -- --list {{filter=\"\"}}",
    "dev": "cargo run -- --help",
    "run:doctor": "cargo run -- --doctor",
    "run:interactive": "cargo run -- --interactive",
    "run:version": "cargo run -- --version"
  }
}